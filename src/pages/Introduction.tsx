import React from 'react';

import { ISession } from '../../types/interfaces';

export const Introduction = ({ session }: { session: ISession | null }) => {
    return (
        <>
        
        </>
    );
}

export default Introduction;